<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="170dp" />
    <stroke
        android:width="2dp"
        android:color="?attr/buttonBackground" />
    <solid android:color="@android:color/transparent" />
</shape>
